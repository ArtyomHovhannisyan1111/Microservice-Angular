server:
  port: 8099

spring:
  application:
    name: image-service
  servlet:

    multipart:
      max-file-size: 15MB
      max-request-size: 15MB

minio:
  endpoint: http://localhost:9000
  access-key: ROOTUSER
  secret-key: CHANGEME123
  bucket-name: order-images
  region: us-east-1